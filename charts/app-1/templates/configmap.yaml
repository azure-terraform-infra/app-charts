apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app-1.fullname" . }}
  labels:
    {{- include "app-1.labels" . | nindent 4 }}
data:
  app.conf: |
    # Application configuration
    APP_NAME: {{ include "app-1.fullname" . }}
    VERSION: {{ .Chart.AppVersion }}
