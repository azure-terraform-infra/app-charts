apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app-3.fullname" . }}
  labels:
    {{- include "app-3.labels" . | nindent 4 }}
data:
  app.conf: |
    # Application configuration
    APP_NAME: {{ include "app-3.fullname" . }}
    VERSION: {{ .Chart.AppVersion }}
